import { Box, Text } from "@chakra-ui/react";

export const Footer = () => {
  return (
    <Box p={5} mt={20} display={"flex"} justifyContent={"center"}>
      <Text color={"gray.600"}>Copyright@Food247</Text>
    </Box>
  );
};
